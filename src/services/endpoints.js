const Endpoints = {
  exampleServiceUrl: 'http://localhost:3333',
  mapasServiceName: 'https://example.com.br/v1/service',
};

function getEndpoints() {
  const endpoints = Endpoints;

  return endpoints;
}

export default getEndpoints();
